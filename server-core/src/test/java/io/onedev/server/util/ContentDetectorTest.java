package io.onedev.server.util;

import org.junit.Test;

public class ContentDetectorTest {

	@Test
	public void detectCharset() {
		/*
		try (var is = Resources.getResource(ContentDetectorTest.class, "charset-detect1").openStream()) {
			var bytes = IOUtils.toByteArray(is);
			ContentDetector.detectCharset(bytes);
		} catch (IOException e) {
			throw new RuntimeException(e);
		}
		 */
	}
	
}